#include<stdio.h>
#include<assert.h>
#include<windows.h>


//one
int my_strlen(const char *str)
{
	int count = 0;
	while(*str++)
	{
		count++;
	}
	return count;
}

//two
int my_strlen(char * str)
{
	assert(str);
	char*end = str;
	while (*end != '\0')
	{
		end++;
	}
	return end-str;
}

int main()
{
	char str[] = "Day day up!";
	int ret = my_strlen(str);
	printf("ret = %d\n",ret);
	system("pause");
	return 0;
}
